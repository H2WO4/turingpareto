procs:
  build:
    shell: cargo build
    autostart: false
  copy:
    shell: cp -f target/debug/turingpareto .
    autostart: false
  build-release:
    shell: cargo build --release
    autostart: false
  copy-release:
    shell: cp -f target/release/turingpareto .
    autostart: false
